@model List<WebService.ViewModel.CompanyViewModel>

@Html.ActionLink("Create", "Create", "Company")
<table class="table table-responsive">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Address</th>
            <th></th>
        </tr>
    </thead>

    @foreach (var company in Model)
    {
        <tr>
            <td>@company.Id</td>
            <td>@company.Name</td>
            <td>@company.Address</td>
            <td>
                <div class="col-md-6">
                    @Html.ActionLink("Edit", "Edit", "Company", new { id = company.Id }, new { @class = "btn btn-primary" })
                </div>
                <div class="col-md-6">
                    @using (Html.BeginForm("Delete", "Company", new { id = company.Id }, FormMethod.Post))
                    {
                        <button class="btn btn-danger" type="submit">Delete</button>
                    }
                </div>
            </td>
        </tr>
    }
</table>